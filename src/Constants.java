public enum Constants {
    INITIAL ("Write action (buy, fill, take, remaining, exit):"),
    WATER ("water");

    public final String string;

    Constants(String string) {
        this.string = string;
    }
}
